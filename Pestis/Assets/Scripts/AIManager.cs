using UnityEngine;

/// <summary>
///  Responsible for managing all AI in the game. Calls out to individual AI scripts to give rough commands (e.g. aggression levels)
/// </summary>
public class AIManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
